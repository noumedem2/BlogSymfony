{% extends 'base.html.twig' %}

{% block title "Change Password" %}

{% block body %}
  <div class="">
    <h1>
      Change Password
    </h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Update Password</button>
    {{ form_end(form) }}
  </div>
{% endblock %}
